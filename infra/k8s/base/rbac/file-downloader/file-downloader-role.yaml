apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: file-downloader-role
  namespace: ytr
rules:
  - apiGroups: [""]
    resources: ["secrets", "configmaps"]
    resourceNames: ["file-downloader-secret"] 
    verbs: ["get"]